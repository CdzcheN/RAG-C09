# data/interim/ —— 构造中间产物

本目录存放**挑战集构造过程的中间文件**（临时切分、算子施加前后的对照、干扰段候选池等），
用于调试与人工抽检复核。

- **不入库**（`.gitignore` 已忽略本目录）；
- 最终冻结产物写到 `data/processed/`，不要把中间态当作交付物；
- 中间文件命名建议带构造参数与种子，如 `retrieval_failure_n2_seed1003.jsonl`，
  便于复现某次具体构造（参数定义见 `docs/数据构造规范.md` §2、§4）。
